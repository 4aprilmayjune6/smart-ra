"""SMART-RA — DART 공시 기반 감사 위험평가 자동화 플랫폼 (참조구현).

RFP v1.2의 설계 원칙을 코드로 구현한 Phase 0/1 범위의 참조구현.

핵심 설계 원칙(RFP 8.1 / 3.5):
- 판정은 서버 결정론 엔진이 전담한다(모듈 1~4). LLM은 조회·서술만 담당.
- 모든 위험신호는 근거(evidence)를 필수로 가진다(NFR-REL-01).
- 동일 스냅샷·동일 룰 버전 → 동일 산출(결정론적 재현성, NFR-REL-02).
"""

__version__ = "0.1.0"
RULE_VERSION = "risk-rules@0.1.0"
