# OpenDART 인증키 (실데이터 모드). 미설정 시 fixture(샘플) 모드로 자동 동작.
# 발급: https://opendart.fss.or.kr/  → 인증키 신청/관리
# 실제 키는 절대 커밋하지 말 것. 로컬은 이 파일을 .env 로 복사해 채우거나 셸에서 export.
SMARTRA_DART_API_KEY=

# 선택: 수집 소스 강제 (auto | fixture | rest). 기본 auto(키 있으면 rest).
# SMARTRA_DART_SOURCE=auto
